#include <bits/stdc++.h>

#define _GLIBCXX_DEBUG

using namespace std;

int main(){
    int a, b, c;
    string str;

    cin >> a >> b >> c;
    cin >> str;

    cout << (a + b + c) << " " << str << endl; 
}